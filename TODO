* addRequire() and delRequire() methods?

* setPhpFlag(), getPhpFlag(), getPhpValue() and setPhpValue() methods?
  These can currently be handled via setAdditional() but could be considered
  as a nice addition.

* Consider using file locking ala File_Passwd
